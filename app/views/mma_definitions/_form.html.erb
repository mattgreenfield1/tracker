<%= form_for(@mma_definition) do |f| %>
  <% if @mma_definition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mma_definition.errors.count, "error") %> prohibited this mma_definition from being saved:</h2>

      <ul>
      <% @mma_definition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
