<%= form_for(@weightlifting_definition) do |f| %>
  <% if @weightlifting_definition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weightlifting_definition.errors.count, "error") %> prohibited this weightlifting_definition from being saved:</h2>

      <ul>
      <% @weightlifting_definition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
